// Local dependencies.
import { TableRowAttrs } from './types';

/**
 * Placeholder content defaults when inserting a new Table Row module.
 */
export const placeholderContent: TableRowAttrs = {
  module: {
    meta: {
      adminLabel: {
        desktop: {
          value: 'Table Row',
        },
      },
    },
  },
};